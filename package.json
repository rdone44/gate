{
  "name": "github-actions-gate",
  "version": "0.1.0",
  "description": "GitHub Actions Gate — verifies that a delivery (task + commit + CI run + test report) satisfies every release gate rule.",
  "type": "module",
  "private": true,
  "bin": {
    "github-actions-gate": "bin/github-actions-gate.js"
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "example:offline": "node examples/offline.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
